  /*    const finalData = pjData.map((item) => {
                console.log(item)
                return {
                    name: item.name,
                    desc: item.desc,
                    stats: item.stats.map((item) => {
                        return item.name;
                    }).join(', ')
                };
            }); */